{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-tetris",
      "type": "shell",
      "command": "g++",
      "args": [
        "-std=c++11",
        "-Wall",
        "${workspaceFolder}/*.cpp",
        "-o",
        "${workspaceFolder}/tetris.exe"
      ],
      "group": { "kind": "build", "isDefault": true }
    }
  ]
}